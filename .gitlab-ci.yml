image: elixir:1.4-slim

services:
  - postgres:9.5

variables:
  MIX_ENV: "test"

before_script:
  - apt-get update
  - apt-get install -y postgresql-client
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get --only test
  - mix ecto.reset

test:
  script:
    - mix test
